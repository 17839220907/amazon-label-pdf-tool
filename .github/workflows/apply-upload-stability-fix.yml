name: Apply upload stability fix

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch Streamlit app
        run: python .github/scripts/apply_upload_stability_fix.py
      - name: Remove one-time files
        run: |
          rm -f .github/workflows/apply-upload-stability-fix.yml
          rm -f .github/scripts/apply_upload_stability_fix.py
      - name: Commit patched app
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add streamlit_app.py .github/workflows/apply-upload-stability-fix.yml .github/scripts/apply_upload_stability_fix.py
          git diff --cached --quiet && exit 0
          git commit -m "Use sequential Feishu uploads and clear finished jobs"
          git push
